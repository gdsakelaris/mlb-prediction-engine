# MLB Prediction Engine — pinned environment.
# Interpreter: Python 3.13 (the daily job and all tools run on
# C:\Users\gdsak\AppData\Local\Programs\Python\Python313\python.exe).
# Install:  python -m pip install -r requirements.txt
#
# Versions pinned 2026-07-19 from the working install. Upgrade deliberately
# (one package at a time, then run Scrapers/validate_data.py and a full
# Scrapers/update_all.py) — the 6 AM job must never break on a silent
# dependency bump.

requests==2.32.5
beautifulsoup4==4.13.5
pandas==2.3.2
numpy==2.3.2
pyarrow==25.0.0
openpyxl==3.1.5
scikit-learn==1.7.1
joblib==1.5.2
Pillow==11.3.0
